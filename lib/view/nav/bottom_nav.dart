// home_page.dart
import 'package:flutter/material.dart';

class HomePage extends StatelessWidget {
  const HomePage({super.key});

  @override
  Widget build(BuildContext context) => Center(child: Text('Home Page'));
}

// resume_scan_page.dart
class ResumeScanPage extends StatelessWidget {
  const ResumeScanPage({super.key});

  @override
  Widget build(BuildContext context) => Center(child: Text('Resume Scan Page'));
}

// profile_page.dart
class ProfilePage extends StatelessWidget {
  const ProfilePage({super.key});

  @override
  Widget build(BuildContext context) => Center(child: Text('Profile Page'));
}

// watch_later_page.dart
class WatchLaterPage extends StatelessWidget {
  const WatchLaterPage({super.key});

  @override
  Widget build(BuildContext context) => Center(child: Text('Watch Later Page'));
}
